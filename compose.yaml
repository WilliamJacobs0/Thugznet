services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: thugzcation
      POSTGRES_USER: thugzcation
      POSTGRES_PASSWORD: local-development
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql

volumes:
  postgres-data:
